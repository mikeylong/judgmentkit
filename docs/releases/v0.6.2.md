# JudgmentKit 0.6.2

Released: 2026-06-30

JudgmentKit 0.6.2 is a patch release for the MCP evidence-routing and AI-native design-system guidance fixes.

## Fixes

- Clarifies UI implementation evidence routing so approved implementation primitives stay separate from design-system component and pattern contract evidence.
- Keeps the AI-native design-system first-use and canonical examples aligned with the current release target.
- Aligns public MCP route handling so `/mcp` and `/mcp/` resolve to the hosted MCP endpoint.
- Updates public release verification to compare live MCP metadata against the package version instead of a newly hard-coded release number.
